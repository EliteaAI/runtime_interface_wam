[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --import-mode=importlib
markers =
    unit: Pure function tests, no DB or Pylon runtime
    integration: Tests requiring plugin module loading with stubs
    e2e: Interface + engine + dummy provider end to end, over a real socket

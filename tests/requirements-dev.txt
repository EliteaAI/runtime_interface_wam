pytest>=7.0.0
pyyaml>=6.0
# Real packages: routes/proxy.py and methods/proxy.py import these directly, unstubbed.
flask>=2.0.0
werkzeug>=2.0.0
# e2e only: the engine half calls the provider with it.
requests>=2.0.0
